run with 
"docker-compose up -d -build"

if permission dennied while trying to connect cause maybe you use linux try using 
"sudo docker-compose up -d -build"

stop with 
"docker-compose down"

